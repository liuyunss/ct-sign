# ============================================================
# CT-Sign (.gitignore)
# 原则：只提交"代码 + 模板 + 平台配置(URL/正则，无密钥)"。
# 凡测试产生的、含真实凭证/本地产物的文件一律忽略。
# ============================================================

# ---------- Python 缓存 / 字节码 ----------
__pycache__/
*.py[cod]
*$py.class
*.so

# ---------- 本地虚拟环境（如你在项目内建了 venv） ----------
venv/
.venv/
env/
ENV/

# ---------- 本地密钥 & 测试用真实 Cookie ----------
# config.py 会自动加载仓库根的 .env（本地调试放 Cookie 用），绝不提交！
.env
.env.*
*.local.yml
*.secret.*

# ---------- 真实全局配置（由 config/config.example.yml 复制而来，可含代理等） ----------
# 注意：config/config.example.yml 是模板，要提交；只有真实的 config/config.yml 忽略。
config/config.yml

# ---------- 平台本地覆盖配置（测试时临时改的，不提交；platforms/<名>/config.yml 是交付物会提交） ----------
platforms/*/config.local.yml

# ---------- 日志 & 运行产物 ----------
*.log
logs/
data/
output/
tmp/
.cache/

# ---------- 编辑器 / 系统文件 ----------
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ---------- 测试时可能随手建的临时文件 ----------
cookies.txt
*.bak
*.tmp
*.orig

# ---------- WorkBuddy 项目数据（防止误提交） ----------
.workbuddy/
